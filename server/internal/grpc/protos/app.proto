syntax = "proto3";

service App {
  rpc RunInference (Request) returns (Response) {}
  rpc RunTraining (Request) returns (Response) {}
  rpc StatusCheck (Request) returns (Response) {}
}

message Request {
  string request = 1;
}

message Response {
  string response = 1;
}
